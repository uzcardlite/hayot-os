# Railway Postgres bazasidan olinadi (Railway dashboard > Postgres service > Variables)
DATABASE_URL="postgresql://user:password@host:port/railway"

# NextAuth uchun maxfiy kalit: `openssl rand -base64 32` bilan generatsiya qiling
AUTH_SECRET=""

# Telegram bot (keyingi bosqichda ishlatiladi)
TELEGRAM_BOT_TOKEN=""
